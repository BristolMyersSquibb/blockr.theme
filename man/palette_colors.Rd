% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{palette_colors}
\alias{palette_colors}
\alias{palette_ramp}
\alias{palette_pals}
\title{Named colour palettes}
\usage{
palette_colors(
  n = NULL,
  palette = "Blockr",
  alpha = NULL,
  recycle = FALSE,
  names = FALSE,
  defs = NULL
)

palette_ramp(n, palette = "Blues", alpha = NULL, rev = FALSE, defs = NULL)

palette_pals(type = NULL, defs = NULL)
}
\arguments{
\item{n}{Number of colours. For \code{palette_colors()}, \code{NULL} returns the
whole palette; asking for more than it holds warns and truncates unless
\code{recycle = TRUE}.}

\item{palette}{Palette name (see \code{palette_pals()}).}

\item{alpha}{Opacity in \verb{[0, 1]}; passed through to \code{grDevices}.}

\item{recycle}{Cycle the palette when \code{n} exceeds its length? Defaults to
\code{FALSE}, matching base: reusing a hue for a second series makes two
things look like one.}

\item{names}{Return the palette with its level names, when it has any.}

\item{defs}{Optional named list of extra definitions (a theme's
\code{palette_defs}), consulted before this package's registry.}

\item{rev}{Reverse the ramp (dark to light).}

\item{type}{Restrict to \code{"qualitative"}, \code{"sequential"}, \code{"diverging"} or
\code{"identity"}; \code{NULL} lists everything.}
}
\value{
A character vector of hex colours. \code{palette_pals()} returns a
character vector of palette names.
}
\description{
\code{palette_colors()} returns a fixed qualitative set and \code{palette_ramp()} an
interpolating one, mirroring \code{\link[grDevices:palette]{grDevices::palette.colors()}} and
\code{\link[grDevices:palettes]{grDevices::hcl.colors()}} in both signature and behaviour. Names this
package does not define fall through to \code{grDevices}, so every base palette
stays reachable; \code{palette_pals()} lists what is available.
}
\details{
The split matters beyond familiarity. \code{palette_colors()} is prefix-stable
(asking for three colours gives the first three of eight), so a level keeps
its colour when the set of levels shown changes. \code{palette_ramp()}
re-interpolates for each \code{n}, so every colour moves. Use the former for
identity, the latter for magnitude.

Palettes this package adds:
\describe{
\item{\code{"Blockr"}}{Six-colour qualitative set (Okabe-Ito without black and
yellow, which fall outside the usable lightness band).}
\item{\code{"Many"}}{240 colours ordered by farthest-point packing, for
many-level identity variables such as subject ids. A prefix is the
best-separated subset. Past roughly 14 entries these are wallpaper:
identity has to come from interaction, not from the colour.}
\item{\code{"Blues"}, \code{"Purples"}, \code{"Greens"}, \code{"Reds"}, \code{"Oranges"},
\code{"Greys"}}{Single-hue sequential ramps over the darker half of the
corresponding base ramp, so the light end stays visible against the
page. Good to about six steps.}
}
}
\examples{
palette_colors(3)                       # first three of the Blockr set
identical(palette_colors(3), palette_colors(6)[1:3])   # prefix-stable
palette_ramp(5, "Blues")
palette_colors(4, "Okabe-Ito")          # falls through to grDevices
head(palette_pals("sequential"))
}
