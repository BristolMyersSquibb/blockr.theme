% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-map.R
\name{new_scale_map}
\alias{new_scale_map}
\alias{scale_binding}
\alias{is_scale_map}
\alias{as_scale_map}
\alias{resolve_scales}
\alias{board_scale_map}
\alias{new_scale_map_option}
\title{Scale map: study-wide level aesthetics}
\usage{
new_scale_map(..., palette = NULL)

scale_binding(var, color = NULL, shape = NULL, linetype = NULL)

is_scale_map(x)

as_scale_map(x)

resolve_scales(map, var, levels, palette = NULL)

board_scale_map()

new_scale_map_option(map = new_scale_map(), category = "Scales", ...)
}
\arguments{
\item{...}{For \code{new_scale_map()}: \code{scale_binding()} objects, \code{scale_map}
objects or plain lists of the same shape (later entries win by variable
name). For \code{new_scale_map_option()}: forwarded to
\code{\link[blockr.core:new_board_options]{blockr.core::new_board_option()}}.}

\item{palette}{Fallback pool for the \code{color} channel when neither the
binding nor the map carries one (typically the renderer's default
colors); \code{shape}/\code{linetype} have no fallback pool}

\item{var}{Variable (column) name the binding applies to}

\item{color, shape, linetype}{Channel vectors: named = fixed values per
level (names are always matched as character, also for numeric-looking
levels such as AE grades \code{"1"}–\code{"5"}), unnamed = pool for stable-hash
auto-assignment. \code{shape} is coerced to integer (R \code{pch} / symbol codes).}

\item{x}{Object to coerce / test}

\item{map}{Initial map value (e.g. a template catalog amended with study
bindings)}

\item{levels}{Character vector of levels actually shown (pass
\code{levels(col)} for factors, \code{unique(as.character(col))} otherwise)}

\item{category}{Settings sidebar category}
}
\value{
\code{new_scale_map()} and \code{as_scale_map()} return a \code{scale_map} object
(a named list of bindings); \code{scale_binding()} returns a \code{scale_binding};
\code{resolve_scales()} returns a list with entries \code{color}, \code{shape},
\code{linetype} (named vectors over the supplied levels; absent when nothing
resolves) and \code{order} (character), or \code{NULL} for an unregistered
variable; \code{new_scale_map_option()} returns a \code{board_option}.
}
\description{
A scale map binds variable levels to aesthetics (colors, shapes, line
types) board-wide: every rendering block that consumes the map shows the
same level in the same color. Bindings are keyed by variable name. Each
channel is one vector: a \emph{named} vector fixes values per level (stated in
display order), an \emph{unnamed} vector is a pool from which unmatched levels
are assigned by a stable hash of the level name (consistent across views,
sessions and data refreshes). Levels of a \code{color} channel not covered by
either fall back to the map's board palette, then to the palette supplied
at resolution time (typically the renderer's default colors).
}
\details{
\code{new_scale_map()} accepts bindings and whole maps in any mix and flattens
them with later-wins-by-variable semantics, so a study overrides a default
catalog by listing replacement bindings after it. Overriding replaces the
whole binding (no channel-level merge).
}
