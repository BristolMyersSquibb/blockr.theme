% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{bundled_font}
\alias{bundled_font}
\title{Bundle a web font with a theme}
\usage{
bundled_font(
  family,
  file,
  package = "blockr.theme",
  weight = "100 900",
  style = "normal"
)
}
\arguments{
\item{family}{CSS family name to register the face under (e.g. \code{"Inter"}).}

\item{file}{Font file within \code{inst/fonts} (a \code{.woff2} is expected).}

\item{package}{Package holding the font (default this one).}

\item{weight, style}{\code{font-weight} / \code{font-style} descriptors; the default
\code{"100 900"} weight declares a variable face spanning the range.}
}
\value{
A \code{blockr_webfont} object consumed by \code{\link[=blockr_theme]{blockr_theme()}}.
}
\description{
Describes a font file shipped in a package's \code{inst/fonts} (this package by
default). \code{\link[=theme_css]{theme_css()}} inlines it as a base64 \verb{@font-face} data URI, so
the font travels inside the theme's \verb{<style>} with no external request or
Shiny resource path -- the same self-contained approach as the token rule.
}
