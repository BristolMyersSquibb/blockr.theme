% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{blockr_theme}
\alias{blockr_theme}
\alias{is_blockr_theme}
\title{Board look-and-feel theme}
\usage{
blockr_theme(
  name,
  chrome = list(),
  exhibits = list(),
  palettes = list(),
  palette_defs = list(),
  scales = NULL,
  templates = list(),
  font_family = NULL,
  webfont = NULL,
  description = NULL
)

is_blockr_theme(x)
}
\arguments{
\item{name}{Short theme name (e.g. \code{"BMS"}).}

\item{chrome}{Named list of UI token overrides. Names are token stems
appended to \verb{--blockr-} (\code{"color-primary"}, \code{"blue-600"}, ...); a few
friendly aliases are accepted (\code{primary}, \code{primary_hover}, \code{primary_bg},
\code{border}, \code{text}). Values are any CSS colour. Overriding the base scale
(\code{blue-600}) plus the semantic layer (\code{color-primary}) shifts the whole
cascade, since the semantic tokens reference the scale.}

\item{exhibits}{Named list of \verb{blockr.viz.*} option values (the leading
\code{blockr.viz.} is added), e.g. \code{list(ft_header_bg = c(...))}. Also honoured
here: \code{ft_font}, the typeface static tables are set in. Name it when the
theme's deck template uses a face of its own -- otherwise a rendered deck
picks the template's font up at render time, but the on-screen table does
not.}

\item{palettes}{Named list mapping a colour ROLE to a palette name (or to a
literal colour vector). Roles are \code{categorical} (series identity),
\code{identity} (many-level pools such as subject ids), \code{sequential},
\code{diverging} and \code{bands} (table header / emphasis fills). Unset roles fall
back to the blockr defaults, so a theme names only what it changes. See
\code{\link[=theme_palette]{theme_palette()}}.}

\item{palette_defs}{Named list of palette definitions this theme
contributes, each a colour vector or a \verb{function(n)}. They become
available to \code{\link[=palette_colors]{palette_colors()}} / \code{\link[=palette_ramp]{palette_ramp()}} and to the scale-map
editor's palette picker for as long as the theme is applied, without
mutating any global registry.}

\item{scales}{Optional \code{\link[=new_scale_map]{new_scale_map()}} (or plain list of that shape).}

\item{templates}{Named list of document templates keyed by format
(\code{pptx}, \code{docx}, \code{quarto}, ...). Each value is a \code{\link[=pkg_template]{pkg_template()}} /
\code{\link[=local_template]{local_template()}} pointer or a plain file path.}

\item{font_family}{Optional CSS font-family stack applied board-wide.
When \code{webfont} is set and this is \code{NULL}, it is derived as the webfont
family followed by a system fallback stack.}

\item{webfont}{Optional bundled web font (see \code{\link[=bundled_font]{bundled_font()}}). Its
\verb{@font-face} is inlined into the theme's CSS as a base64 data URI, so
the font renders without any external request or resource-path setup.}

\item{description}{Optional one-line human description.}

\item{x}{Object to test / operate on.}
}
\value{
\code{blockr_theme()} returns a \code{blockr_theme} object.
}
\description{
A \code{blockr_theme} bundles the house style of a board: UI chrome tokens,
rendered-exhibit palettes, an optional data scale map, document templates
and a font. It is consumed through additive seams (see \code{\link[=use_theme]{use_theme()}},
\code{\link[=theme_head]{theme_head()}}, \code{\link[=theme_template]{theme_template()}}, \code{\link[=theme_scale_map_option]{theme_scale_map_option()}}); a board
with no theme, or a theme missing a facet, simply falls back to the
blockr defaults.
}
\examples{
th <- blockr_theme(
  name = "demo",
  chrome = list("blue-600" = "#BE2BBB", "color-primary" = "#BE2BBB"),
  exhibits = list(ft_header_bg = c(.stub = "#EEE", "#33D6F1"))
)
cat(theme_css(th))
}
