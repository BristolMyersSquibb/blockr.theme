% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_css}
\alias{theme_css}
\title{Compile a theme's chrome to a CSS rule}
\usage{
theme_css(x, selector = ":root")
}
\arguments{
\item{x}{A \code{\link[=blockr_theme]{blockr_theme()}}.}

\item{selector}{CSS selector the rule is scoped to. \verb{:root} themes the
whole page; a wrapper class (e.g. \code{.theme-bms}) scopes it to a subtree,
which is how two differently themed boards could share one page.}
}
\value{
A CSS string (length-1 character), or \code{""} if the theme sets no
chrome tokens and no font.
}
\description{
Emits the token overrides as a single custom-property rule on \code{selector}
(\verb{:root} by default). Because every consumer reads the tokens with a
fallback, this rule is all a chrome theme is: redefine the properties on
a scope that wins the cascade (source order after the base CSS).
}
