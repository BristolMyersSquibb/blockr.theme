% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_palette}
\alias{theme_palette}
\alias{current_theme}
\title{The theme's palette for a colour role}
\usage{
theme_palette(role, n = NULL, theme = current_theme())

current_theme()
}
\arguments{
\item{role}{One of \code{categorical}, \code{identity}, \code{sequential}, \code{diverging},
\code{bands}.}

\item{n}{Number of colours. \code{NULL} returns the palette's own length for a
fixed set, and is an error for a ramp.}

\item{theme}{A \code{\link[=blockr_theme]{blockr_theme()}}; defaults to the one \code{\link[=use_theme]{use_theme()}} /
\code{\link[=apply_theme_options]{apply_theme_options()}} last applied, or none.}
}
\value{
A character vector of hex colours, or \code{NULL} when the role has no
value and no default (\code{bands}).
}
\description{
Resolves what colours to use for a given \emph{job}, which is the vocabulary a
theme is written in: \code{categorical} for series identity, \code{identity} for
many-level pools (subject ids), \code{sequential} for magnitude, \code{diverging} for
polarity, \code{bands} for table header and emphasis fills.
}
\details{
Consumers ask by role and never name a palette, so one theme entry
recolours every renderer that shares the role. With no theme applied the
blockr defaults answer, so a themeless board is unchanged.
}
\examples{
theme_palette("categorical", 3)
theme_palette("sequential", 5, theme = theme_blockr())
}
